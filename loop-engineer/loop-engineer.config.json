{
  "$schema": "见 ARCHITECTURE.md",
  "watchDirs": ["../fde-copilot/clients"],
  "pollIntervalMs": 15000,
  "maxAttempts": 3,
  "providers": {
    "planner": "hilinkup:glm-5.2",
    "coder": "deepseek",
    "reviewer": "hilinkup:kimi-k2.5"
  },
  "notes": "纯云配置(HiLinkup 主力 + DeepSeek):planner/reviewer 走 HiLinkup(OpenAI 兼容,一 key 多模型);coder 必须 Anthropic 兼容端点(HiLinkup 驱动不了 agentic claude -p),用 deepseek(api.deepseek.com/anthropic)。要用本机 claude 订阅可把某角色改回 'claude';无 key 本地跑通编排可改 'mock'。watchDirs 下任意含 loop.json 且有 todo 任务的目录会被当作 job。"
}
